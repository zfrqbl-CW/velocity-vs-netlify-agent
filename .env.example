GROQ_API_KEY=your_groq_api_key
PORT=3000

# Only set this on the Netlify deployment. Point it at the public /log
# route of your Velocity deployment so step events land in one place.
# Leave it unset on Velocity itself.
LOG_ENDPOINT=
